CREATE TABLE dbo.Teacher
(
	ID INT IDENTITY,
	SSN VARCHAR(11) NOT NULL,
	FirstName VARCHAR(25) NOT NULL,
	LastName VARCHAR(25) NOT NULL,
	Active BIT NOT NULL,
	CONSTRAINT PK_Teacher PRIMARY KEY (ID),
	CONSTRAINT UX_Teacher_1 UNIQUE (SSN)
);